#!/usr/bin/env groovy

node {
    stage('Build and release') {
        sh 'echo "Building and releasing..."'
    }

    stage('Publish') {
        sh 'echo "Publishing.."'
    }

    stage('Deploy') {
        sh 'echo "Deploying..."'
    }
}
